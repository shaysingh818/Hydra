
//! # Generic environment abstractions
//! The environment module is a collection of abstractions that apply.
//! The module includes components for creating a `State` structure and adding
//! agents to it. Agents are represented using the `Agent` structure..
pub mod agent; 
pub mod state; 
// pub mod test_environment; 